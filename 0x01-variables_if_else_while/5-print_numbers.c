#include <stdio.h>

/**
 * main - prints numbers 0 to 9.
 *
 * Return: always 0 (Succees)
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
