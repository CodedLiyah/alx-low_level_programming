#include <stdio.h>
/*
 * main - single digit numbers of base 10 starting from 0.
 *
 * Return: always 0 (Succees)
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
