#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if the code is uppercase
 * @c: is the int used for the function
 * Return: 1 if uppercase character 0 if not
 */
int_isupper(int c);
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
